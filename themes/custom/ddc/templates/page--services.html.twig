{% include 'themes/custom/ddc/templates/includes/header.html.twig' %}

<div class="inner-banner">
  <div class="container">
    <div>
      <div class="banner-text">
        <h1 class="text-uppercase animated slideInUp">{{ node.title.value|raw }}</h1>
        <p class="text-faded mb-5 animated slideInUp">{{ page.breadcrumb }}</p>
      </div>
    </div>
  </div>
  <div class="slide-image"> <img src="{{ file_url(node.field_banner_image.entity.fileuri) }}" alt="{{node.field_banner_image.alt}}" /></div>
</div>


{% if node.field_title.value %}
  <div class="layout-intro text-center">
      <div class="container p-lg-5">
        <h2>{{ node.field_title.value|raw }}</h2>
        <div class="body col-md-10 m-auto">
          {{ node.body.value|raw }}
        </div>
      </div>
  </div>
{% endif %}


{% for item in node.field_suggestions %}
  {% if loop.index == 1 %}
    <div class="layout1 py-lg-5">
      <div class="image-background" style="background:url({{ file_url(item.entity.field_image.entity.fileuri) }}) no-repeat; background-size: cover;">
        <h2 class="background-block">{{ item.entity.field_title.value|raw }}</h2>
        <div class="body col-md-7">
          {{ item.entity.field_banner_description.value|raw }}
        </div>
      </div>
    </div>
  {% elseif loop.index == 2 %}
    <div class="layout2 py-lg-5">
      <div class="container">
        <div class="row pt-lg-5">
          <div class="col-md-7">
            <h2>{{ item.entity.field_title.value|raw }}</h2>
            <div class="body">
              {{ item.entity.field_banner_description.value|raw }}
            </div>
          </div>
          <div class="col-md-5">
            <img src="{{ item.entity.field_image.entity.uri.value | image_style('portfolio') }}" alt="{{item.entity.field_image.alt}}"/>
            {# <img src="{{ file_url(item.entity.field_image.entity.fileuri) }}" /> #}
          </div>
        </div>
      </div>
    </div>
  {% elseif loop.index == 3 %}
    <div class="layout3 py-lg-5">
      <div class="container">
        <div class="row pt-lg-5">
          <div class="col-md-5">
            <img src="{{ item.entity.field_image.entity.uri.value | image_style('portfolio') }}" alt="{{item.entity.field_image.alt}}"/>
            {# <img src="{{ file_url(item.entity.field_image.entity.fileuri) }}" /> #}
          </div>
          <div class="col-md-7">
            <h2>{{ item.entity.field_title.value|raw }}</h2>
            <div class="body">
              {{ item.entity.field_banner_description.value|raw }}
            </div>
          </div>
        </div>
      </div>
    </div>
  {% elseif loop.index == 4 %}
    <div class="layout4 py-lg-5">
      <div class="image-background" style="background:url({{ file_url(item.entity.field_image.entity.fileuri) }}) no-repeat; background-size: cover;">
        <h2 class="">{{ item.entity.field_title.value|raw }}</h2>
        <div class="body col-md-7">
          {{ item.entity.field_banner_description.value|raw }}
        </div>
      </div>
    </div>
  {% elseif loop.index == 5 %}
    <div class="layout5 py-lg-5">
      <div class="container">
        <div class="row pt-lg-5">
          <div class="col-md-7">
            <h2>{{ item.entity.field_title.value|raw }}</h2>
            <div class="body">
              {{ item.entity.field_banner_description.value|raw }}
            </div>
          </div>
          <div class="col-md-5">
            <img src="{{ item.entity.field_image.entity.uri.value | image_style('portfolio') }}" alt="{{item.entity.field_image.alt}}"/>
            {# <img src="{{ file_url(item.entity.field_image.entity.fileuri) }}" /> #}
          </div>
        </div>
      </div>
    </div>
  {% elseif loop.index == 6 %}
    <div class="layout6 py-lg-5">
      <div class="container">
        <div class="row pt-lg-5">
          <div class="col-md-5">
            <img src="{{ item.entity.field_image.entity.uri.value | image_style('portfolio') }}" alt="{{item.entity.field_image.alt}}"/>
            {# <img src="{{ file_url(item.entity.field_image.entity.fileuri) }}" /> #}
          </div>
          <div class="col-md-7">
            <h2>{{ item.entity.field_title.value|raw }}</h2>
            <div class="body">
              {{ item.entity.field_banner_description.value|raw }}
            </div>
          </div>
        </div>
      </div>
    </div>
  {% endif %}
{% endfor %}

{# <div class="layout2"></div>
<div class="layout3"></div>
<div class="layout4"></div>
<div class="layout5"></div>
<div class="layout6"></div> #}
{# Services paragraphs #}
<div class="content-container">
  <div class="container">
    {# <div class="content-section section-12 text-center">
      <div class="row">
        <div class="col-md-10 mx-auto p-5">
          <h1>{{ node.field_block_title.value }}</h1>
          {{ node.field_description.value|raw }}
        </div>
      </div>
    </div> #}
    <section class="service-list">
      <div class="container">
        <div class="services-block pt-5">
          <div class="row">
            {% for item in node.field_services_icons %}
              <div class="col-md-4">
                <div class="s-block">
                  <div class="icon-image">
                    <img src="{{ file_url(item.entity.field_image.entity.fileuri) }}" alt="{{item.entity.field_image.alt}}" />
                  </div>
                  <h6>{{ item.entity.field_title.value|raw }}</h6>
                </div>
              </div>
            {% endfor %}
          </div>
        </div>
      </div>
    </section>


  </div>
  <div class="experience">
    <div class="exp-bg" style="background-image: url({{ file_url(node.field_image.entity.fileuri) }}); width: 100%;">
      <div class="container">
        <div class="">
          <h2>{{ node.field_block_title.value|raw }}</h2>
          {{ node.field_description.value|raw }}
          <a href="#" class="btn">Get in Touch</a>
        </div>
      </div>
    </div>
  </div>
</div>




{% include 'themes/custom/ddc/templates/includes/footer.html.twig' %}
